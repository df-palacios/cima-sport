# Déjalo comentado para desarrollo: la app deduce sola la API del host desde
# el que se abre (sirve en localhost y desde el celular por la red local).
# VITE_API_URL=http://localhost:4001/api

# Producción detrás de Nginx, en subcarpeta:
# VITE_BASE=/proyectos/cima-sport/
# VITE_API_URL=/proyectos/cima-sport/api
